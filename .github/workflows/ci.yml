name: ci

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: |
            requirements.txt
            requirements-dev.txt
            pyproject.toml

      - name: Install
        run: |
          python -m pip install -U pip setuptools wheel
          pip install -r requirements.txt -r requirements-dev.txt
          pip install -e .

      - name: Ruff
        run: |
          python -m ruff format --check .
          python -m ruff check . --output-format=github

      - name: Pytest (coverage gate)
        run: |
          python -m pytest -q --cov=src --cov-fail-under=75

      - name: Security (pip-audit)
        continue-on-error: true
        run: |
          pip-audit -r requirements.txt --skip-editable

  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Build test stage
        run: |
          docker build --target test -t fraud-risk-ops:test .

      - name: Docker Compose smoke test
        run: |
          docker compose up -d --build
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:8000/live; then break; fi
            sleep 2
          done
          curl -fsS http://127.0.0.1:8000/ready
          curl -fsS http://127.0.0.1:8000/metrics | head -n 5

      - name: Stop Docker Compose
        if: always()
        run: |
          docker compose down -v
